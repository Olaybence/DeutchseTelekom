import { Observable } from "rxjs";
import { Post } from "src/services/post.service";

// Not used at the end, but left here the place for further ideas.
export class GlobalVar {
    public static action: string;
}